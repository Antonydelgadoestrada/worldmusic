'use client';

import { useEffect } from 'react';

export default function HomeClientAnimations() {
  useEffect(() => {
    // Aquí se pueden inicializar animaciones o librerías del lado del cliente
    // si fuese necesario en el futuro.
  }, []);

  return null;
}
